# server/docs/ — server-local deep dives

Too long for `../CLAUDE.md`; loaded on demand from its § Read when.

| File | Read when |
|---|---|
| `modules.md` | Adding a feature module or route group. |
| `db.md` | Changing the Drizzle schema or generating a migration. |

API map, request/DI flow and the env table live in `../README.md`. Anything that also
touches the client or the engine belongs in `../../docs/`.
