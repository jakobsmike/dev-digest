# reviewer-core/docs/ — engine deep dives

Too long for `../CLAUDE.md`; loaded on demand from its § Read when.

Empty for now: pipeline and public API live in `../README.md`, prompt anatomy in
`../../docs/agent-prompts/README.md` (written for prompt authors, not engine maintainers).

Add a file when engine detail outgrows `../CLAUDE.md` — a worked map-reduce example, or
grounding rules for a new finding `kind` — and link it from § Read when in the same commit.
